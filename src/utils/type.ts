//types that will be used in more than one file
export type TableDataType = {
  name: string;
  independent: boolean;
  region: string;
  population: number;
  startOfWeek: string;
  unMember: boolean;
  languages: string;
  area: number;
  capitalInfo: String;
  currencies: String;
};
